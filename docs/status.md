# Project status

## Done
- 2026-07-19: AI-dev setup installed (AGENTS.md, Playwright UI checks, Claude agents/hooks).

## In progress / remaining
- Uncommitted on main: AI-dev setup files (AGENTS.md, CLAUDE.md, docs/, .claude/,
  playwright.config.ts, tests/ui/, scripts/ui-check.sh) and desktop-window-guard
  changes + tests — needs a branch + PR to land (pushing to main is blocked).

## Known issues
-
